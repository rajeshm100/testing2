HTML and CSS Practice Questions
HTML Practice Questions
Create a simple HTML page with a title "My First Page".

Add a heading <h1> with the text "Hello World".

Insert a paragraph with the text "This is my first paragraph".

Add a horizontal line between two paragraphs.

Create a comment in HTML that says "This is a comment".

Display text in bold using <b> and <strong>.

Display text in italic using <i> and <em>.

Add a subscript and superscript to the text H2O and x².

Create a blockquote with the text "Practice makes perfect".

Add a line break between two sentences.

Create an ordered list of three fruits.

Create an unordered list of three vegetables.

Nest an ordered list inside an unordered list.

Use a description list (<dl>) for Coffee and Milk with descriptions.

Create a list with custom numbering starting from 5.

Create a hyperlink to https://www.google.com.

Make a link open in a new tab.

Add an image with alt text "Sample Image".

Resize an image to 200px width and 150px height.

Make an image clickable as a link.

Create a table with 2 rows and 2 columns.

Add a table header row with "Name" and "Age".

Merge two cells using colspan.

Merge two rows using rowspan.

Add a border to the table.

Create a form with text input and a submit button.

Add a password field to the form.

Add radio buttons for gender selection.

Add checkboxes for hobbies.

Create a dropdown menu with three options.

CSS Practice Questions
Change the background color of the page to lightblue.

Change the text color of all paragraphs to green.

Use an ID selector to style one heading.

Use a class selector to style multiple paragraphs.

Apply multiple styles (color, font-size, font-family) to a paragraph.

Change the font-family of a paragraph to Arial.

Make text bold using CSS.

Add letter-spacing of 5px to a heading.

Add word-spacing of 1cm to a paragraph.

Align text to the center of the page.

Add a 2px solid black border around a div.

Add padding of 20px inside a div.

Add margin of 30px around a div.

Create a box with different border colors on each side.

Add a shadow to text using text-shadow.

Float an image to the right of the text.

Use position: absolute to place a div at the top-right corner.

Create a flexbox container with three items.

Use CSS Grid to create a 2x2 layout.

Make a div resizable vertically with resize: vertical.